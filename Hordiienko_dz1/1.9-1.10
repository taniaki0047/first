//1.10
#include <iostream>
#include <stdio.h>
#include <math.h>

using namespace std;

int main()
{
    printf("  x  |   1  |   2  |   3  |   4  |   5   \n");
    printf("---- + ---- + ---- + ---- + ---- + ----  \n");
    printf("F(x) | %.2f | %.2f | %.2f | %.2f | %.2f", sqrt(1), sqrt(2), sqrt(3), sqrt(4), sqrt(5));

}


//1.9
#include <iostream>
#include <stdio.h>
#include <math.h>

using namespace std;

int main()
{
    float x;
    printf("input x = ");
    scanf("%f", &x);
    x = x*x;
    x = x*x;
    x = x*x;
    printf("res = %f", x);

}

